<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Android App Links: https://app.pomocare.com からネイティブアプリを起動 -->
            <!-- autoVerify="true" + assetlinks.json でブラウザを経由せず直接起動 -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="app.pomocare.com" />
            </intent-filter>

            <!-- カスタムスキーム: com.pomocare.app://auth フォールバック -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.pomocare.app" android:host="auth" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Google AdMob -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="@string/admob_app_id"/>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Notifications (Android 13+ runtime prompt) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration for Haptics plugin -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Exact alarms for timer accuracy (Android 12+) -->
    <!-- SCHEDULE_EXACT_ALARM: Android 12 端末の後方互換として残置（削除しない） -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- USE_EXACT_ALARM: Android 13+ (API33+) でインストール時に自動付与・ユーザー無効化不可。
         timer app / alarm clock app として適格。canScheduleExactAlarms()=true が全ユーザーで保証され
         LocalNotifications が setExactAndAllowWhileIdle 経路に入る（画面オフでも ~0秒発火）。 -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- Wake device to show notification -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Re-schedule alarms after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
</manifest>
