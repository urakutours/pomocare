# PomoCare LP モダンリデザイン設計書

> **ステータス:** 設計フェーズ
> **最終更新:** 2026-02-22
> **対象:** `lp/` フォルダ内の公式WEBサイト全体

---

## 1. 現状分析

### 1.1 現行サイト構成

```
lp/
├── index.html          メインLP（493行）
├── css/style.css       全スタイル（915行, バニラCSS）
├── js/main.js          DOM操作・アニメーション（110行）
├── js/i18n.js          日英翻訳システム（279行, 252キー）
├── images/             SVGイラスト11点 + スクリーンショットJPG13点
├── privacy/index.html  プライバシーポリシー（日英バイリンガル）
├── support/index.html  サポートページ（日英バイリンガル）
├── terms/index.html    利用規約（日英バイリンガル）
└── manual/index.html   ユーザーマニュアル（1,503行, 13セクション）
```

### 1.2 現行技術スタック

| カテゴリ | 現行 |
|---------|------|
| マークアップ | HTML5 |
| スタイリング | バニラCSS（フレームワーク無し） |
| JavaScript | バニラJS（ES6+） |
| フォント | Google Fonts（Inter + Noto Sans JP） |
| アイコン | インラインSVG |
| i18n | カスタム data-i18n 属性 + JSオブジェクト |
| アニメーション | CSS transitions + IntersectionObserver |
| ビルドツール | なし（静的HTML） |

### 1.3 現行の課題

| # | 課題 | 詳細 |
|---|------|------|
| 1 | **CSSアーキテクチャ** | 915行の単一ファイル。CSS変数未使用、テーマ切替不可 |
| 2 | **ビジュアルの単調さ** | 全セクションが同じカードグリッド+ホバーリフトの繰り返し |
| 3 | **ヒーローの訴求力不足** | 静的なフォンモックアップ、wave dividerが2020年代前半的 |
| 4 | **アニメーション不足** | fade-inのみ。スクロール連動やマイクロインタラクション無し |
| 5 | **ダークモード非対応** | メインアプリはダーク対応済みだがLPは非対応 |
| 6 | **セクション間の視覚的変化が乏しい** | 背景色の交互切替のみで単調 |
| 7 | **比較テーブルの視認性** | モバイルでの可読性が低い |
| 8 | **CTAの弱さ** | CTAセクションが小さく、コンバージョン導線が弱い |
| 9 | **スクリーンショットの未活用** | 13枚のスクリーンショットがLP上に表示されていない |
| 10 | **保守性の低さ** | CSS変数・mixin等の再利用パターンが無く、変更時の影響範囲が広い |

---

## 2. デザインコンセプト

### 2.1 コンセプトキーワード

**「Calm Focus, Modern Craft」**

- **Calm（静寂）** — 集中を助けるアプリにふさわしい落ち着いたトーン
- **Focus（焦点）** — 視線誘導が明確で、情報の優先度がひと目でわかる
- **Modern（現代的）** — 2025-2026年のWEBトレンドを適切に取り入れる
- **Craft（丁寧さ）** — ディテールに宿る品質感。マイクロインタラクションや余白の美しさ

### 2.2 デザインの方向性

```
現行: フラット × カードグリッド × ミニマル
  ↓
新規: レイヤード × Bento Grid × インタラクティブ
```

- **フラット → レイヤード**: 背景の奥行き、Glassmorphism、微かなシャドウのレイヤー
- **均一グリッド → Bento Grid**: 不均等なグリッドで視覚的リズムを作る
- **静的 → インタラクティブ**: スクロール連動アニメーション、ホバーでの表情変化

### 2.3 参考にするデザイントレンド（2025-2026）

| トレンド | 適用方法 |
|---------|---------|
| **Bento Grid** | Feature セクションを不均等グリッドで構成 |
| **Glassmorphism** | ヘッダー、モーダル、フローティング要素に適用 |
| **Scroll-driven Animation** | セクション遷移やデータの視覚化に活用 |
| **Large Typography** | ヒーローで超大型タイポグラフィを使用 |
| **Gradient Mesh** | ヒーロー背景に多色のメッシュグラデーション |
| **Micro Interactions** | ボタン、カード、トグルの細やかなフィードバック |
| **Dark Mode** | システム設定連動 + 手動トグル |

---

## 3. デザインシステム

### 3.1 カラーパレット

SCSS変数として `_variables.scss` で一元管理する。

```scss
// =============================================
// カラーパレット — Primary
// =============================================
$color-primary:        #0abab5;     // Tiffany（メインアプリと統一）
$color-primary-hover:  #099d99;
$color-primary-light:  #ccf2f1;
$color-primary-subtle: #f0fffe;

// =============================================
// カラーパレット — Neutral
// =============================================
$color-gray-950:  #0a0f1a;
$color-gray-900:  #111827;
$color-gray-800:  #1f2937;
$color-gray-700:  #374151;
$color-gray-600:  #4b5563;
$color-gray-500:  #6b7280;
$color-gray-400:  #9ca3af;
$color-gray-300:  #d1d5db;
$color-gray-200:  #e5e7eb;
$color-gray-100:  #f3f4f6;
$color-gray-50:   #f9fafb;

// =============================================
// カラーパレット — Accent
// =============================================
$color-accent-purple: #7c3aed;
$color-accent-amber:  #f59e0b;
$color-accent-rose:   #f43f5e;
$color-accent-sky:    #0ea5e9;

// =============================================
// カラーパレット — Semantic
// =============================================
$color-success:  #10b981;
$color-warning:  #f59e0b;
$color-error:    #ef4444;
```

テーマ切替はCSS Custom Propertiesで行い、SCSSからCSS変数として出力する。

```scss
// =============================================
// テーマ — Light（デフォルト）
// =============================================
:root {
  --surface-bg:       #{$white};
  --surface-raised:   #{$white};
  --surface-overlay:  rgba(255, 255, 255, 0.80);
  --surface-sunken:   #{$color-gray-50};

  --text-primary:    #{$color-gray-900};
  --text-secondary:  #{$color-gray-600};
  --text-tertiary:   #{$color-gray-400};
  --text-inverse:    #{$white};
}

// =============================================
// テーマ — Dark
// =============================================
[data-theme="dark"] {
  --surface-bg:       #{$color-gray-950};
  --surface-raised:   #151c2c;
  --surface-overlay:  rgba(15, 20, 35, 0.85);
  --surface-sunken:   #0d1320;

  --text-primary:    #{$color-gray-100};
  --text-secondary:  #{$color-gray-400};
  --text-tertiary:   #{$color-gray-500};
  --text-inverse:    #{$color-gray-900};
}
```

### 3.2 タイポグラフィ

```scss
// =============================================
// フォントファミリー
// =============================================
$font-sans:    'Inter', 'Noto Sans JP', -apple-system, BlinkMacSystemFont, sans-serif;
$font-display: 'Inter', 'Noto Sans JP', sans-serif;
$font-mono:    'JetBrains Mono', 'Fira Code', monospace;

// =============================================
// フォントサイズスケール
// clamp() によるフルイドタイポグラフィを採用
// =============================================
$text-xs:   0.75rem;                            // 12px
$text-sm:   0.875rem;                           // 14px
$text-base: 1rem;                               // 16px
$text-lg:   1.125rem;                           // 18px
$text-xl:   1.25rem;                            // 20px
$text-2xl:  1.5rem;                             // 24px
$text-3xl:  1.875rem;                           // 30px
$text-4xl:  clamp(2rem, 4vw, 2.5rem);          // 32-40px
$text-5xl:  clamp(2.5rem, 5vw, 3.25rem);       // 40-52px
$text-hero: clamp(2.75rem, 6vw, 4.5rem);       // 44-72px（ヒーロー用）

// =============================================
// 行間 (line-height)
// =============================================
$leading-tight:   1.2;
$leading-snug:    1.4;
$leading-normal:  1.7;
$leading-relaxed: 1.9;

// =============================================
// フォントウェイト
// =============================================
$font-normal:    400;
$font-medium:    500;
$font-semibold:  600;
$font-bold:      700;
$font-extrabold: 800;
```

### 3.3 スペーシング

```scss
// =============================================
// スペーシングスケール
// 4pxベースの一貫したスペーシングシステム
// =============================================
$space-1:  4px;
$space-2:  8px;
$space-3:  12px;
$space-4:  16px;
$space-5:  20px;
$space-6:  24px;
$space-8:  32px;
$space-10: 40px;
$space-12: 48px;
$space-16: 64px;
$space-20: 80px;
$space-24: 96px;
$space-32: 128px;

// =============================================
// セクション余白（レスポンシブ対応）
// =============================================
$section-py: clamp(64px, 10vw, 120px);
```

### 3.4 角丸・シャドウ・エフェクト

```scss
// =============================================
// 角丸 (Border Radius)
// =============================================
$radius-sm:   8px;
$radius-md:   12px;
$radius-lg:   16px;
$radius-xl:   20px;
$radius-2xl:  24px;
$radius-full: 9999px;

// =============================================
// シャドウ
// 段階的な深さを表現するシャドウシステム
// =============================================
$shadow-xs:   0 1px 2px rgba(0, 0, 0, 0.05);
$shadow-sm:   0 2px 8px rgba(0, 0, 0, 0.06);
$shadow-md:   0 4px 16px rgba(0, 0, 0, 0.08);
$shadow-lg:   0 8px 32px rgba(0, 0, 0, 0.10);
$shadow-xl:   0 16px 48px rgba(0, 0, 0, 0.12);
$shadow-glow: 0 0 40px rgba(10, 186, 181, 0.15);

// =============================================
// Glassmorphism エフェクト
// =============================================
$glass-bg:     rgba(255, 255, 255, 0.60);
$glass-border: rgba(255, 255, 255, 0.25);
$glass-blur:   16px;

// =============================================
// トランジション
// =============================================
$ease-out:    cubic-bezier(0.16, 1, 0.3, 1);
$ease-spring: cubic-bezier(0.34, 1.56, 0.64, 1);
$duration-fast:   150ms;
$duration-normal: 300ms;
$duration-slow:   500ms;
```

### 3.5 ブレークポイント

```scss
// =============================================
// レスポンシブ ブレークポイント
// モバイルファーストで設計
// =============================================
$bp-sm:  640px;   // スマートフォン横
$bp-md:  768px;   // タブレット
$bp-lg:  1024px;  // デスクトップ
$bp-xl:  1280px;  // ワイドスクリーン
```

---

## 4. SCSS アーキテクチャ

### 4.1 採用理由

| 利点 | 詳細 |
|------|------|
| **変数 ($variable)** | カラー・サイズ・スペーシングを一元管理。変更時の影響を局所化 |
| **Mixin** | レスポンシブ、Glassmorphism、カードスタイル等の再利用パターンをDRYに |
| **Nesting** | BEM的なセレクタをネストで直感的に記述 |
| **Partials & @use** | ファイル分割でセクション別に管理、@useでスコープ付きインポート |
| **コメント (//)** | コンパイル後に残らない開発者向けコメントで視認性向上 |
| **計算 (math)** | スペーシングの算出やグリッドの計算をSCSS内で完結 |

### 4.2 ビルドフロー

```
[SCSS ソース]                    [CSS 出力]
lp/scss/                    →    lp/css/
├── main.scss               →    ├── style.css        (結合・圧縮済み)
├── _variables.scss                └── style.css.map   (ソースマップ)
├── _mixins.scss
├── _reset.scss
├── _components.scss
├── _header.scss
├── _hero.scss
├── _features.scss
├── _showcase.scss
├── _use-cases.scss
├── _how-it-works.scss
├── _pricing.scss
├── _faq.scss
├── _cta.scss
├── _footer.scss
├── _animations.scss
└── _dark.scss
```

**ビルドコマンド:**

```bash
# 開発（watch + ソースマップ付き）
npx sass --watch lp/scss/main.scss:lp/css/style.css --source-map

# 本番（圧縮）
npx sass lp/scss/main.scss:lp/css/style.css --style=compressed --no-source-map
```

> **Note**: `sass`（Dart Sass）を devDependencies に追加する。Node Sass は非推奨。

### 4.3 SCSS ファイル構成詳細

#### `main.scss` — エントリーポイント

```scss
// =============================================
// PomoCare LP — メインスタイルシート
// =============================================
// このファイルはすべてのパーシャルを結合するエントリーポイントです。
// 個別のスタイルはそれぞれのパーシャルファイルで管理します。
// =============================================

// --- 基盤 ---
@use 'variables' as *;
@use 'mixins' as *;
@use 'reset';

// --- 共通コンポーネント ---
@use 'components';

// --- セクション（表示順） ---
@use 'header';
@use 'hero';
@use 'features';
@use 'showcase';
@use 'use-cases';
@use 'how-it-works';
@use 'pricing';
@use 'faq';
@use 'cta';
@use 'footer';

// --- テーマ・アニメーション ---
@use 'animations';
@use 'dark';
```

#### `_mixins.scss` — 再利用可能なミキシン

```scss
// =============================================
// Mixin 定義
// 再利用可能なスタイルパターンを集約
// =============================================

// ----- レスポンシブ -----
// 使用例: @include mq(md) { ... }
@mixin mq($breakpoint) {
  @if $breakpoint == sm {
    @media (min-width: $bp-sm) { @content; }
  } @else if $breakpoint == md {
    @media (min-width: $bp-md) { @content; }
  } @else if $breakpoint == lg {
    @media (min-width: $bp-lg) { @content; }
  } @else if $breakpoint == xl {
    @media (min-width: $bp-xl) { @content; }
  }
}

// ----- max-width メディアクエリ（モバイルターゲット用） -----
// 使用例: @include mq-down(md) { ... }
@mixin mq-down($breakpoint) {
  @if $breakpoint == sm {
    @media (max-width: #{$bp-sm - 1px}) { @content; }
  } @else if $breakpoint == md {
    @media (max-width: #{$bp-md - 1px}) { @content; }
  } @else if $breakpoint == lg {
    @media (max-width: #{$bp-lg - 1px}) { @content; }
  }
}

// ----- Glassmorphism エフェクト -----
// 半透明＋ブラー背景のガラス効果
// 使用例: @include glass;
// 使用例: @include glass(0.8, 20px);
@mixin glass($opacity: 0.6, $blur: $glass-blur) {
  background: rgba(255, 255, 255, $opacity);
  backdrop-filter: blur($blur);
  -webkit-backdrop-filter: blur($blur);  // Safari対応
  border: 1px solid $glass-border;
}

// ----- カードスタイル -----
// 標準的なカードの外観を適用
// 使用例: @include card;
// 使用例: @include card($radius-xl, $shadow-md);
@mixin card($radius: $radius-lg, $shadow: $shadow-sm) {
  background: var(--surface-raised);
  border: 1px solid $color-gray-200;
  border-radius: $radius;
  box-shadow: $shadow;
  transition: all $duration-normal $ease-out;
}

// ----- カードホバー -----
// カードのホバー状態を適用
// 使用例: @include card-hover;
@mixin card-hover {
  border-color: $color-primary;
  box-shadow: $shadow-lg;
  transform: translateY(-4px);
}

// ----- セクション共通パディング -----
// 使用例: @include section-padding;
@mixin section-padding {
  padding: $section-py 0;
}

// ----- コンテナ -----
// 中央揃え + 最大幅制限
// 使用例: @include container;
// 使用例: @include container(900px);
@mixin container($max-width: 1120px) {
  max-width: $max-width;
  margin: 0 auto;
  padding: 0 $space-6;

  @include mq-down(sm) {
    padding: 0 $space-4;
  }
}

// ----- フレックスセンタリング -----
// 使用例: @include flex-center;
@mixin flex-center {
  display: flex;
  align-items: center;
  justify-content: center;
}

// ----- テキスト切り詰め -----
// 使用例: @include truncate;
// 使用例: @include truncate(2); // 2行で切り詰め
@mixin truncate($lines: 1) {
  @if $lines == 1 {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  } @else {
    display: -webkit-box;
    -webkit-line-clamp: $lines;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
}

// ----- セクションタイトル -----
// 使用例: @include section-title;
@mixin section-title {
  font-size: $text-4xl;
  font-weight: $font-bold;
  text-align: center;
  margin-bottom: $space-3;
  color: var(--text-primary);
}

// ----- セクションサブタイトル -----
// 使用例: @include section-subtitle;
@mixin section-subtitle {
  text-align: center;
  color: var(--text-secondary);
  font-size: $text-lg;
  margin-bottom: $space-16;
}

// ----- スムーズトランジション -----
// 使用例: @include transition(color, background);
@mixin transition($properties...) {
  $value: ();
  @each $prop in $properties {
    $value: append($value, $prop $duration-normal $ease-out, comma);
  }
  transition: $value;
}

// ----- フォーカス表示（アクセシビリティ） -----
// 使用例: @include focus-ring;
@mixin focus-ring {
  &:focus-visible {
    outline: 2px solid $color-primary;
    outline-offset: 2px;
  }
}

// ----- レデューストモーション対応 -----
// アニメーション無効化ラッパー
// 使用例: @include reduced-motion { animation: none; }
@mixin reduced-motion {
  @media (prefers-reduced-motion: reduce) {
    @content;
  }
}

// ----- ダークモード用の色上書き -----
// 使用例: @include dark { background: $color-gray-900; }
@mixin dark {
  [data-theme="dark"] & {
    @content;
  }
}
```

### 4.4 SCSS コメント規約

ファイル全体の視認性を高めるため、以下のコメント規約を適用する:

```scss
// =============================================
// セクション区切り（ファイルレベルの大見出し）
// =============================================

// ----- サブセクション（機能グループの中見出し） -----

// 単行コメント（プロパティ・セレクタの補足説明）

// TODO: 未実装・要対応事項
// HACK: 暫定対応（理由を併記）
// NOTE: 重要な補足情報
```

**実例:**

```scss
// =============================================
// ヒーローセクション
// メインビジュアルエリア。ファーストビューで最も重要。
// =============================================

.hero {
  position: relative;
  padding: 120px 0 $space-20;
  overflow: hidden;

  // ----- 背景グラデーション -----
  // メッシュグラデーション: Tiffany + 淡いパープル + 暖色を混合
  background:
    radial-gradient(ellipse at 20% 50%, rgba($color-primary, 0.15) 0%, transparent 50%),
    radial-gradient(ellipse at 80% 20%, rgba($color-accent-purple, 0.08) 0%, transparent 50%),
    radial-gradient(ellipse at 60% 80%, rgba($color-accent-amber, 0.06) 0%, transparent 50%),
    linear-gradient(135deg, $color-primary-subtle 0%, #fff 100%);

  // ----- 内部レイアウト -----
  &-inner {
    @include container;
    display: flex;
    align-items: center;
    gap: $space-16;

    @include mq-down(md) {
      flex-direction: column;
      text-align: center;
      gap: $space-10;
    }
  }

  // ----- テキストエリア -----
  &-text {
    flex: 1;
    max-width: 520px;

    h1 {
      font-size: $text-hero;
      font-weight: $font-extrabold;
      line-height: $leading-tight;
      margin-bottom: $space-5;
      color: var(--text-primary);
    }
  }
}
```

---

## 5. ページ構成と各セクション設計

### 5.1 セクション構成（リデザイン後）

| # | セクション | 目的 | レイアウト変更 |
|---|-----------|------|--------------|
| 1 | **Header** | ナビゲーション | Glassmorphism + スクロール時の縮小アニメーション |
| 2 | **Hero** | 第一印象・CTA | メッシュグラデーション背景 + アニメーション付きモックアップ + 大胆なタイポグラフィ |
| 3 | **Social Proof** | 信頼性 | **新規追加** — 数字で実績を見せるカウンターセクション |
| 4 | **Features** | 機能紹介 | Bento Grid レイアウト（不均等）+ インタラクティブなアイコン |
| 5 | **App Showcase** | アプリの魅力 | **新規追加** — スクリーンショットのカルーセル/ギャラリー |
| 6 | **Use Cases** | 利用シーン | タブ切替式（以前は4カード並列） |
| 7 | **How It Works** | 使い方 | 水平タイムライン + スクロール連動プログレス |
| 8 | **Pricing** | 料金プラン | カード＋年/月トグル + 比較表を統合 |
| 9 | **FAQ** | よくある質問 | アコーディオン（デザイン改善） |
| 10 | **CTA** | 最終コンバージョン | フルワイド + カウントダウン風テキスト演出 |
| 11 | **Footer** | フッター | マルチカラム + SNSリンク |

### 5.2 各セクション詳細設計

---

#### 5.2.1 Header

```
┌─────────────────────────────────────────────────────────────┐
│  [▶ PomoCare]    機能  料金  FAQ    [☀/🌙] [🌐JP▾] [アプリを開く] │
└─────────────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- Glassmorphism: `@include glass` で半透明白背景 + ブラー
- スクロール時に高さ 64px → 56px に縮小（CSS transition）
- スクロール時にシャドウが出現
- モバイル: ハンバーガー → フルスクリーンメニュー（底から上へスライド）
- ダークモードトグルをナビに追加

**インタラクション:**
- ロゴ: ホバーで微回転
- ナビリンク: 下線アンダーラインがスライドイン（左→右）
- アクティブセクション自動ハイライト（IntersectionObserver）

**SCSS構造:**
```scss
// _header.scss
.header {
  // ... 固定ヘッダー
  @include glass;

  &.scrolled { /* スクロール時の縮小 */ }

  .nav { /* ナビゲーションレイアウト */ }
  .nav-links { /* デスクトップリンク群 */ }
  .mobile-nav { /* モバイルメニュー */ }
  .theme-toggle { /* ダーク/ライト切替 */ }
  .lang-dropdown { /* 言語ドロップダウン */ }
}
```

---

#### 5.2.2 Hero

```
┌──────────────────────────────────────────────────────────┐
│  ░░ Gradient Mesh Background ░░░░░░░░░░░░░░░░░░░░░░░░░░ │
│                                                          │
│   深い集中を、              ┌────────────────┐           │
│   もっとシンプルに。        │   📱 Phone     │           │
│                             │   Mockup with  │           │
│   PomoCareは...             │   animation    │           │
│                             │                │           │
│   [🚀 今すぐ無料で使う]     │   18:45        │           │
│   [Google Play] [MS Store]  │                │           │
│                             └────────────────┘           │
│   ✓ 登録不要  ✓ 完全無料  ✓ 広告あり                     │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- **背景**: CSS gradient mesh（Tiffany + 淡いパープル + 薄いアンバーを混合した多点グラデーション）
- **タイポグラフィ**: `$text-hero` + `font-weight: 800` で超大型
- **フォンモックアップ**: タイマーの円形プログレスが実際にアニメーション（CSS animation）
- **Wave divider を廃止** → セクション間はグラデーションフェードで自然に遷移
- **Trust Badges**: 「登録不要」「完全無料」などをチップで横並び表示

**アニメーション:**
- テキスト: 1文字ずつフェードイン（staggered animation）
- モックアップ: 下から浮き上がり + 微かなフロート（infinite）
- CTA ボタン: 微かなパルスグロー

---

#### 5.2.3 Social Proof（新規）

```
┌─────────────────────────────────────────────────┐
│                                                 │
│   10,000+        4.8★        3つ                │
│   ユーザー       評価         プラットフォーム    │
│                                                 │
└─────────────────────────────────────────────────┘
```

**デザイン仕様:**
- 3カラムのカウンターセクション
- 数字はスクロール時にカウントアップアニメーション
- 背景: `var(--surface-sunken)`
- コンパクトに（padding控えめ）

> **NOTE**: 実際の数字はサービスの成長に合わせて更新する。初期は控えめな表現にするか、セクション自体を非表示にしておく選択肢もあり。

---

#### 5.2.4 Features（Bento Grid）

```
┌──────────────────────────────────────────────────────┐
│                                                      │
│  ┌──────────────────────┐  ┌───────────┐             │
│  │  ⏱ ポモドーロタイマー │  │  🏷 ラベル │             │
│  │  (2x1 大きいカード)   │  │  (1x1)    │             │
│  │  アニメーション付き   │  │           │             │
│  └──────────────────────┘  └───────────┘             │
│  ┌───────────┐  ┌───────────┐  ┌───────────────────┐ │
│  │ 📊 統計   │  │ 💻 マルチ │  │ 🛡 プライバシー   │ │
│  │ (1x1)     │  │ (1x1)     │  │ & クラウド同期    │ │
│  │           │  │           │  │ (2x1)             │ │
│  └───────────┘  └───────────┘  └───────────────────┘ │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- **Bento Grid**: `grid-template-columns: repeat(3, 1fr)` + `grid-column: span 2` で不均等
- 大きいカードにはインラインのミニアニメーションやスクリーンショットを配置
- **カードスタイル**: `@include card($radius-xl)` + ホバーで `@include card-hover`
- **アイコン**: 各カード上部に48pxのSVGアイコン

**SCSS構造:**
```scss
// _features.scss
.features {
  @include section-padding;

  &-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: $space-6;

    @include mq-down(lg) { grid-template-columns: repeat(2, 1fr); }
    @include mq-down(md) { grid-template-columns: 1fr; }
  }
}

.feature-card {
  @include card($radius-xl);

  &:hover { @include card-hover; }

  // ----- 2カラム幅のカード -----
  &--wide {
    grid-column: span 2;
    @include mq-down(md) { grid-column: span 1; }
  }
}
```

---

#### 5.2.5 App Showcase（新規）

```
┌────────────────────────────────────────────────────────┐
│                                                        │
│  PomoCareを体験する                                     │
│                                                        │
│  [タイマー] [統計] [設定] [ラベル]  ← タブ切替           │
│                                                        │
│       ┌──────────────────┐                              │
│       │  📸 Screenshot   │  ← 選択タブに対応した         │
│       │  (実際のアプリ    │    スクリーンショット表示      │
│       │   スクリーンショット)│                            │
│       └──────────────────┘                              │
│                                                        │
│  右側にキャプション・説明テキスト                         │
│                                                        │
└────────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- 既存の13枚のスクリーンショットを活用
- タブで切り替え、選択中のスクリーンショットはフォンモックアップ内に表示
- **アニメーション**: タブ切替時にクロスフェード

**スクリーンショットのグループ分け:**

| タブ | 使用する画像 |
|------|-------------|
| タイマー | screenshot-focus-mode.jpg, screenshot-break-start.jpg, screenshot-break-running.jpg |
| 統計 | screenshot-stats-weekly.jpg, screenshot-stats-monthly.jpg, screenshot-stats-yearly.jpg |
| 設定 | screenshot-settings.jpg, screenshot-settings-bottom.jpg, screenshot-settings-presets.jpg |
| ラベル | screenshot-labels.jpg, screenshot-label-add.jpg |

---

#### 5.2.6 Use Cases

**変更:** 4カード並列 → タブ / ピルナビ切替式

```
┌────────────────────────────────────────────────────┐
│                                                    │
│  [📚 受験] [🌍 語学] [💻 デスクワーク] [🎨 副業]   │
│                                                    │
│  ┌──────────────────────────────────────────┐      │
│  │ 受験・学業                               │      │
│  │                                          │      │
│  │ 参考書1冊を25分ずつ。ラベルで...          │      │
│  │                                          │      │
│  │ 📊 科目別の学習時間を可視化              │      │
│  └──────────────────────────────────────────┘      │
│                                                    │
└────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- ピルナビでユースケースを切替
- アクティブなピルには `$color-primary` 背景
- コンテンツエリアはフェードで切替

---

#### 5.2.7 How It Works

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│          ①                ②                ③            │
│    ┌──────────┐     ┌──────────┐     ┌──────────┐      │
│    │ タイマーを │ ──→ │  集中する  │ ──→ │ 振り返る │      │
│    │  スタート  │     │          │     │          │      │
│    └──────────┘     └──────────┘     └──────────┘      │
│    ════════════════════════════════▶                     │
│         ↑ スクロール連動プログレスバー                     │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- 水平タイムライン（デスクトップ）/ 垂直タイムライン（モバイル）
- スクロールでプログレスバーが伸びるアニメーション
- ステップアイコンはスクロール到達時にポップイン
- 矢印をプログレスライン（実線）に置き換え

---

#### 5.2.8 Pricing

```
┌──────────────────────────────────────────────────────────┐
│                                                          │
│  料金プラン                                               │
│                                                          │
│  ┌────────────┐  ┌─────────────────┐  ┌────────────┐    │
│  │   Free     │  │   Standard ★    │  │    Pro     │    │
│  │   ¥0       │  │   ¥200/月      │  │  ¥3,800    │    │
│  │            │  │   (おすすめ)    │  │  (一括)    │    │
│  │   ...      │  │   ...          │  │   ...      │    │
│  │ [無料で始める]│ │ [始める]       │  │ [購入する] │    │
│  └────────────┘  └─────────────────┘  └────────────┘    │
│                                                          │
│  ─── 機能比較テーブル（折りたたみ） ───                     │
│  [▼ 詳細な比較を見る]                                      │
│                                                          │
└──────────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- 3カードレイアウト維持
- **Popularカード**: ボーダーグローエフェクト + 微妙に大きいスケール
- **比較テーブル**: 独立セクションを廃止 → Pricing下部に折りたたみで統合
- カード背景: Glassmorphism（特にダークモード時）

---

#### 5.2.9 FAQ

**デザイン仕様:**
- max-width: 720pxのアコーディオン（現行踏襲）
- **改善点**:
  - 開閉時にスムーズな高さアニメーション（`grid-template-rows` トリック）
  - アイコンを `+` → 回転するシェブロンに変更
  - 開いた項目のボーダーカラーが `$color-primary` に変化

---

#### 5.2.10 CTA

```
┌──────────────────────────────────────────────────────┐
│  ▓▓▓ Gradient Mesh Background（Hero と呼応）▓▓▓▓▓▓  │
│                                                      │
│         さあ、集中を始めましょう                       │
│         PomoCareで、より生産的な毎日を。               │
│                                                      │
│         [🚀 無料で始める]                              │
│                                                      │
│   Web ・ Windows ・ Android  対応                     │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- ヒーローと呼応するグラデーション
- ボタンは大きめ + グローエフェクト
- プラットフォームアイコンを下部に横並び

---

#### 5.2.11 Footer

```
┌──────────────────────────────────────────────────────┐
│                                                      │
│  [▶ PomoCare]                                        │
│                                                      │
│  プロダクト      リソース       法的情報               │
│  ・機能          ・マニュアル    ・プライバシー         │
│  ・料金          ・サポート      ・利用規約            │
│  ・FAQ           ・ブログ                              │
│                                                      │
│  ─────────────────────────────────────────────       │
│  © 2026 PomoCare by URAKU Inc.    [🌐JP] [☀/🌙]     │
│                                                      │
└──────────────────────────────────────────────────────┘
```

**デザイン仕様:**
- 3カラム + ボトムバー構成
- ダーク背景（`$color-gray-900`）
- 言語・テーマ切替をフッターにも配置

---

## 6. コンポーネント一覧

### 6.1 共通コンポーネント

| コンポーネント | 説明 | 使用箇所 |
|--------------|------|---------|
| `Button` | Primary / Outline / Ghost / White の4バリアント | 全セクション |
| `SectionHeader` | タイトル + サブタイトルのセット | 各セクション冒頭 |
| `Card` | Glassmorphism対応の汎用カード | Features, Pricing, Use Cases |
| `Badge` | ステータス表示チップ | Pricing, Use Cases |
| `Accordion` | FAQ用折りたたみ | FAQ |
| `PhoneMockup` | アプリスクリーンショット表示枠 | Hero, App Showcase |
| `LanguageDropdown` | 言語切替ドロップダウン | Header, Footer |
| `ThemeToggle` | ダーク/ライト切替 | Header, Footer |
| `TabNav` | タブ / ピルナビゲーション | App Showcase, Use Cases |
| `Counter` | カウントアップ表示 | Social Proof |
| `ComparisonTable` | 機能比較テーブル | Pricing |

### 6.2 ボタンバリアント

```scss
// _components.scss 内のボタン定義
// 使い分け:
//   .btn-primary  — メインCTA（目立たせたい箇所）
//   .btn-outline  — セカンダリCTA
//   .btn-ghost    — テキストリンク風（ナビ内等）
//   .btn-white    — 暗い背景上のCTA

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  font-weight: $font-semibold;
  border-radius: $radius-md;
  border: 2px solid transparent;
  cursor: pointer;
  @include transition(background, color, border-color, box-shadow, transform);
  @include focus-ring;

  // ----- サイズバリアント -----
  &-sm  { padding: 6px 18px;  font-size: $text-sm; }
  &-md  { padding: 10px 24px; font-size: $text-base; } // デフォルト
  &-lg  { padding: 14px 36px; font-size: $text-lg; }

  // ----- カラーバリアント -----
  &-primary {
    background: $color-primary;
    color: #fff;
    border-color: $color-primary;
    &:hover { background: $color-primary-hover; border-color: $color-primary-hover; }
  }

  &-outline {
    background: transparent;
    color: $color-primary;
    border-color: $color-primary;
    &:hover { background: $color-primary; color: #fff; }
  }

  &-ghost {
    background: transparent;
    color: $color-primary;
    &:hover { background: $color-primary-subtle; }
  }

  &-white {
    background: #fff;
    color: $color-primary;
    border-color: #fff;
    &:hover { background: $color-primary-subtle; }
  }

  // ----- マイクロインタラクション -----
  &:hover  { transform: translateY(-1px); }
  &:active { transform: scale(0.97); }
}
```

---

## 7. アニメーション・インタラクション設計

### 7.1 スクロール連動アニメーション

| 要素 | アニメーション | トリガー |
|------|-------------|---------|
| セクションタイトル | フェードイン + 下→上スライド | IntersectionObserver (threshold: 0.2) |
| カード群 | スタガードフェードイン（50ms間隔） | IntersectionObserver |
| Social Proof 数字 | カウントアップ（0→target, 1.5s） | IntersectionObserver |
| How It Works プログレス | 左→右にライン伸長 | Scroll progress |
| スクリーンショット | スケールアップ + フェードイン | IntersectionObserver |

### 7.2 マイクロインタラクション

| 要素 | アニメーション |
|------|-------------|
| ボタン(hover) | 微スケール(1.02) + シャドウ増加 + 背景色変化 |
| ボタン(click) | スケール(0.97) → 戻り（spring easing） |
| カード(hover) | Y軸 -4px + シャドウ増加 + ボーダーカラー変化 |
| ナビリンク(hover) | 下線がスライドイン（左→右） |
| FAQ(open/close) | 高さアニメーション + シェブロン回転 |
| タブ(切替) | アクティブ背景がスライド移動 |
| フォンモックアップ | 常時微フロート（3s infinite） |
| ヒーローテキスト | 1行ずつ stagger フェードイン |
| テーマトグル | 回転 + モーフィング |

### 7.3 SCSS でのアニメーション定義

```scss
// _animations.scss

// =============================================
// キーフレーム定義
// =============================================

// ----- フェードイン + 上方向スライド -----
@keyframes fade-up {
  from {
    opacity: 0;
    transform: translateY(24px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

// ----- フロート（ヒーローモックアップ用） -----
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50%      { transform: translateY(-8px); }
}

// ----- パルスグロー（CTAボタン用） -----
@keyframes pulse-glow {
  0%, 100% { box-shadow: 0 0 0 0 rgba($color-primary, 0.4); }
  50%      { box-shadow: 0 0 20px 4px rgba($color-primary, 0.2); }
}

// ----- タイマープログレス（ヒーローモックアップ用） -----
@keyframes timer-progress {
  from { stroke-dashoffset: 565; }
  to   { stroke-dashoffset: 141; }
}

// =============================================
// アニメーションユーティリティクラス
// =============================================

// ----- スクロールトリガー用の基底クラス -----
.animate-on-scroll {
  opacity: 0;
  transform: translateY(24px);
  transition: opacity $duration-slow $ease-out,
              transform $duration-slow $ease-out;

  &.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  // ----- スタガード対応 -----
  // 子要素に .stagger-item を付与し、JSで --stagger-index を設定
  .stagger-item {
    opacity: 0;
    transform: translateY(16px);
    transition: opacity $duration-normal $ease-out,
                transform $duration-normal $ease-out;
    transition-delay: calc(var(--stagger-index, 0) * 80ms);
  }

  &.is-visible .stagger-item {
    opacity: 1;
    transform: translateY(0);
  }
}

// =============================================
// アクセシビリティ: モーション軽減
// =============================================
@include reduced-motion {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

---

## 8. レスポンシブ戦略

### 8.1 レイアウト変化

| セクション | Mobile (<768px) | Tablet (768-1023px) | Desktop (1024px+) |
|-----------|-----------------|--------------------|--------------------|
| Header | ハンバーガー + フルスクリーンメニュー | ハンバーガー | フルナビ表示 |
| Hero | 縦スタック（テキスト→モックアップ） | 縦スタック | 横並び |
| Features | 1カラム | 2カラム | Bento Grid 3カラム |
| App Showcase | 縦スタック + スワイプ | 横並び | 横並び |
| Use Cases | 縦スタック（タブ維持） | タブ切替 | タブ切替 |
| How It Works | 縦タイムライン | 横タイムライン | 横タイムライン |
| Pricing | 1カラム（Popularが先頭） | 3カラム | 3カラム |
| FAQ | フルワイド | max-width: 720px | max-width: 720px |
| Footer | 縦スタック | 3カラム | 3カラム |

### 8.2 レスポンシブの実装パターン

```scss
// Mixin を使った統一的なレスポンシブ対応例
.features-grid {
  display: grid;
  gap: $space-6;

  // モバイルファースト: 1カラム
  grid-template-columns: 1fr;

  // タブレット: 2カラム
  @include mq(md) {
    grid-template-columns: repeat(2, 1fr);
  }

  // デスクトップ: 3カラム Bento Grid
  @include mq(lg) {
    grid-template-columns: repeat(3, 1fr);
  }
}
```

---

## 9. 技術アーキテクチャ

### 9.1 技術選定

| カテゴリ | 選定 | 理由 |
|---------|------|------|
| **マークアップ** | HTML5（静的） | ビルド不要。SEO最適。デプロイが簡単 |
| **スタイリング** | **SCSS → CSS コンパイル** | 変数・mixin・ネストで保守性向上。CSS Custom Propertiesとの併用でランタイムテーマ切替も実現 |
| **JavaScript** | バニラJS（ES6+） | ライブラリ依存ゼロ。高速。現行i18nシステムを継続活用 |
| **フォント** | Google Fonts（Inter + Noto Sans JP） | 現行踏襲。十分にモダン |
| **アイコン** | インラインSVG | 現行踏襲。HTTPリクエスト削減 |
| **CSSビルド** | **Dart Sass (sass)** | Node Sassは非推奨。Dart Sassが公式推奨 |
| **デプロイ** | GitHub Pages（メインアプリと同居） | 現行踏襲 |

> **重要方針**: メインアプリ（React + Vite + Tailwind）とLPは別アーキテクチャを維持。LPは静的HTMLの利点（高速表示、SEO、シンプルさ）を活かす。

### 9.2 SCSS → CSS ビルドの統合

`package.json` に以下のスクリプトを追加:

```json
{
  "scripts": {
    "lp:dev": "sass --watch lp/scss/main.scss:lp/css/style.css --source-map",
    "lp:build": "sass lp/scss/main.scss:lp/css/style.css --style=compressed --no-source-map"
  },
  "devDependencies": {
    "sass": "^1.83.0"
  }
}
```

### 9.3 SCSS ファイル構成

```
lp/scss/
│
│  // ─── 基盤（変数・mixin・リセット）───
├── main.scss              エントリーポイント（@use で全パーシャルを結合）
├── _variables.scss        SCSS変数 + CSS Custom Properties
├── _mixins.scss           再利用可能なミキシン（レスポンシブ、glass、card等）
├── _reset.scss            リセット・ベーススタイル
│
│  // ─── 共通コンポーネント ───
├── _components.scss       Button, Badge, SectionHeader, TabNav 等
│
│  // ─── セクション別（HTMLの表示順） ───
├── _header.scss           ヘッダー・ナビゲーション
├── _hero.scss             ヒーローセクション
├── _social-proof.scss     Social Proof セクション（新規）
├── _features.scss         機能セクション（Bento Grid）
├── _showcase.scss         App Showcase セクション（新規）
├── _use-cases.scss        ユースケースセクション
├── _how-it-works.scss     使い方セクション
├── _pricing.scss          料金プラン + 比較テーブル
├── _faq.scss              FAQセクション
├── _cta.scss              CTAセクション
├── _footer.scss           フッター
│
│  // ─── テーマ・アニメーション ───
├── _animations.scss       キーフレーム・アニメーションユーティリティ
└── _dark.scss             ダークモード上書き
```

### 9.4 JavaScript ファイル構成

```
lp/js/
├── main.js            エントリーポイント（初期化・イベント登録）
├── i18n.js            翻訳システム（現行ベース、拡張）
├── theme.js           ダーク/ライトモード切替
├── animations.js      スクロールアニメーション・IntersectionObserver
├── tabs.js            タブ切替（App Showcase, Use Cases）
└── counter.js         カウントアップ（Social Proof）
```

---

## 10. アクセシビリティ

| 要件 | 対応 |
|------|------|
| カラーコントラスト | WCAG AA 準拠（4.5:1 以上） |
| キーボード操作 | すべてのインタラクティブ要素がTab/Enterで操作可能 |
| スクリーンリーダー | ARIA属性を適切に使用（ランドマーク、ラベル、ライブリージョン） |
| モーション | `prefers-reduced-motion` でアニメーション無効化（`@include reduced-motion`） |
| フォーカス表示 | `:focus-visible` スタイル（`@include focus-ring`） |
| 言語切替 | `lang` 属性を動的に切替 |
| 画像代替テキスト | すべてのスクリーンショットに `alt` テキスト |

---

## 11. パフォーマンス目標

| 指標 | 目標値 |
|------|-------|
| **LCP (Largest Contentful Paint)** | < 2.0s |
| **FID (First Input Delay)** | < 100ms |
| **CLS (Cumulative Layout Shift)** | < 0.1 |
| **Total Page Size** | < 1.5MB（初回ロード） |
| **Lighthouse Score** | 90+ (Performance, Accessibility, Best Practices, SEO) |

**最適化方針:**
- スクリーンショットのWebP変換 + lazy loading
- フォントの `font-display: swap` + preload
- SCSS→CSSのビルドで圧縮出力
- ソースマップは開発時のみ
- インラインCSSでファーストビューを高速化（Critical CSS）

---

## 12. 実装フェーズ

### Phase 1: 基盤整備
- [ ] `sass` パッケージをdevDependenciesに追加
- [ ] SCSS ファイル構成の作成（`_variables.scss`, `_mixins.scss`, `_reset.scss`）
- [ ] CSS Custom Properties + SCSS変数の定義
- [ ] 既存 `style.css` の内容をSCSSパーシャルに分割・移行
- [ ] `lp:dev` / `lp:build` スクリプトの設定
- [ ] ダークモード基盤（`theme.js` + CSS変数切替）

### Phase 2: Header + Hero リデザイン
- [ ] Glassmorphismヘッダー
- [ ] スクロール時のヘッダー縮小
- [ ] ダーク/ライト テーマトグル
- [ ] ヒーローセクション（メッシュグラデーション + アニメーション）
- [ ] フォンモックアップのリデザイン
- [ ] モバイルナビのリデザイン

### Phase 3: Features + App Showcase
- [ ] Bento Grid レイアウト
- [ ] カードコンポーネントのリデザイン
- [ ] App Showcase セクション新規作成
- [ ] タブ切替コンポーネント（`tabs.js`）

### Phase 4: Use Cases + How It Works + Pricing
- [ ] Use Cases のタブ切替化
- [ ] How It Works のタイムラインリデザイン
- [ ] Pricing カードのリデザイン
- [ ] 比較テーブルの折りたたみ統合

### Phase 5: FAQ + CTA + Footer
- [ ] FAQ アコーディオンの改善
- [ ] CTA セクションのリデザイン
- [ ] Footer のマルチカラム化

### Phase 6: アニメーション + 仕上げ
- [ ] スクロール連動アニメーション実装（`animations.js`）
- [ ] マイクロインタラクション実装
- [ ] Social Proof セクション（`counter.js`）
- [ ] `prefers-reduced-motion` 対応
- [ ] ダークモード最終調整

### Phase 7: 最適化 + QA
- [ ] 画像最適化（WebP変換、lazy loading）
- [ ] Lighthouse テスト・スコア改善
- [ ] クロスブラウザテスト
- [ ] i18n 翻訳キーの追加（新セクション分）
- [ ] レスポンシブ全画面確認

---

## 13. 画像・アセット管理

### 13.1 既存アセット

| ファイル | 用途 | リデザインでの扱い |
|---------|------|------------------|
| favicon.svg | ファビコン | そのまま使用 |
| SVGイラスト（10点） | マニュアル用 | マニュアル内で継続使用 |
| screenshot-*.jpg（13点） | アプリスクリーンショット | App Showcase セクションで活用 |

### 13.2 新規に必要なアセット

| アセット | 用途 | フォーマット |
|---------|------|------------|
| og-image.png | SNSシェア用OGP画像 | PNG 1200x630 |
| スクリーンショット(WebP) | 既存JPGのWebP版 | WebP |

> **方針**: イラストやアイコンは基本的にインラインSVGまたはCSSで作成し、外部画像ファイルを最小限にする。

---

## 14. i18n（国際化）対応

### 14.1 現行システム

- `data-i18n` 属性でキーを指定
- `js/i18n.js` 内の翻訳オブジェクト（ja/en）でテキストを管理
- `localStorage` に言語設定を保持
- ブラウザ言語の自動検出

### 14.2 リデザインで追加が必要な翻訳キー

```
// Social Proof
socialProofUsers, socialProofRating, socialProofPlatforms

// App Showcase
showcaseTitle, showcaseSubtitle
showcaseTabTimer, showcaseTabStats, showcaseTabSettings, showcaseTabLabels
showcaseTimerDesc, showcaseStatsDesc, showcaseSettingsDesc, showcaseLabelsDesc

// Theme Toggle
themeLight, themeDark

// Pricing (comparison toggle)
compToggleExpand, compToggleCollapse

// Footer (追加カラム)
footerProduct, footerResources, footerLegal
```

---

## 15. サブページ（privacy, terms, support, manual）

### 15.1 方針

- **メインLPのリデザインを優先**。サブページはPhase完了後に整合性を取る
- サブページのヘッダー・フッターをメインLPと共通化する
- ダークモードをサブページにも適用する
- 現行のバイリンガル切替は維持

### 15.2 サブページのリデザイン範囲

| ページ | リデザイン内容 |
|--------|-------------|
| privacy | ヘッダー/フッター共通化、タイポグラフィ統一、ダークモード対応 |
| terms | 同上 |
| support | 同上 + FAQコンポーネントの統一 |
| manual | 同上 + 目次UIの改善、スクリーンショットのWebP化 |

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2026-02-22 | 初版作成 — 現状分析、デザインコンセプト、全セクション設計、SCSSアーキテクチャ、Mixin・変数設計、実装フェーズ策定 |
